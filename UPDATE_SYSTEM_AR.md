# نظام التحديث والإشعارات المجاني

أضيف للتطبيق فحص تلقائي لملف تحديث عام. عند فتح التطبيق، ثم كل ست ساعات أثناء بقائه مفتوحاً، يقرأ التطبيق ملف `updates.json`. إذا كان رقم الإصدار الموجود في الملف أكبر من الإصدار الحالي، تظهر نافذة إشعار داخل التطبيق.

## الإعداد مرة واحدة

1. أنشئ مستودعاً عاماً مجانياً على GitHub.
2. انسخ `updates.json.example` إلى المستودع باسم `updates.json` وعدّل محتواه.
3. افتح `src/utils/updateService.ts` وعدّل `UPDATE_MANIFEST_URL` إلى رابط raw لملفك، بصيغة:
   `https://raw.githubusercontent.com/اسمك/اسم-المستودع/main/updates.json`
4. عند كل إصدار جديد، غيّر `version` إلى رقم أعلى، واكتب الرسالة ورابط تحميل APK الجديد، ثم ابنِ APK جديداً.

## مثال ملف التحديث

```json
{
  "version": "1.0.1",
  "title": "تحديث جديد متوفر",
  "message": "تمت إضافة تحسينات وإصلاحات جديدة للتطبيق.",
  "url": "https://example.com/download/latest.apk",
  "publishedAt": "2026-09-24T00:00:00Z",
  "mandatory": false
}
```

`mandatory: true` يمنع إغلاق نافذة الإشعار، لكنه لا يجبر Android على تثبيت APK تلقائياً. التثبيت التلقائي يحتاج نظام توزيع خاص أو Google Play؛ أما هذه الطريقة فهي مجانية وتعرض التنبيه ورابط التحديث فقط.

إذا لم يتم وضع رابط GitHub الحقيقي، يبقى الفحص معطلاً بشكل آمن ولا يؤثر على عمل VPN.
