package com.seloomwarp.vpn

/**
 * Concrete UserService entry point located in the host APK itself.
 * Shizuku resolves this class from the installed application package.
 */
class OodiTetherService : dev.shizzi.TetherService()
